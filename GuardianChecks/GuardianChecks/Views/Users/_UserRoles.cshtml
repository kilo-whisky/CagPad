@model GuardianChecks.Models.UserRolesSelect

@Html.ListBoxFor(x => x.RoleSelected, Model.RoleList.Select(f => new SelectListItem { Value = f.RoleName, Text = f.RoleDescription }), new { ID = "roles" })